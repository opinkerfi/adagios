# INSTALLATION INSTRUCTIONS FOR RHEL6
# ***********************************


# Install yum repos
# =================
rpm -Uvh http://download.fedoraproject.org/pub/epel/6/i386/epel-release-6-7.noarch.rpm
rpm -Uhv http://opensource.is/repo/ok-release-10-1.el6.noarch.rpm


# Install Pre-requisits
# =====================
# Adagios requires nagios to be installed on the local computer.
# okconfig is also a vital part of adagios, and brings in tons
# of plugins and methods to deploy agents.
yum install -y nagios okconfig
yum install -y git # don't ask. just do it


# Turn of selinux
# ===============
# Currently selinux does not allow apache to write to /etc/nagios/ directory
setenforce 0


# Install adagios
# ===============
yum install -y adagios

